# ── Anthropic (required) ─────────────────────────────────────────────
ANTHROPIC_API_KEY=sk-ant-...

# ── Outlook mail sending (optional) ─────────────────────────────────
# Set your SAP email here to enable real mail sending.
# Leave empty to run in mock/demo mode.
# On first run: a code will appear in the terminal — paste it at
#   https://microsoft.com/devicelogin  →  log in  →  done.
# After that: silent refresh, no login needed (~90 days).
#
GRAPH_USER_EMAIL=kristiyan.hadjiev@sap.com
